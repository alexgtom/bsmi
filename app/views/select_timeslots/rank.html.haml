%section
  %h2 Selected Timeslots
  = render :partial => "week_calendar", :locals => { :width => 940, :height => 700, :calendar_id => :rank, :timeslots => @timeslots}

%section
  %h2 Pick Timeslot Preferences
  Please rank which timeslots you prefer from highest to lowest where "1" is your most preferred timeslot.
= form_for @student, url: wizard_path do |f|
  = f.error_messages
  %table.table.table-striped
    %thead
      %tr
        %th Rank
        %th Day
        %th Start Time
        %th End Time
    %tbody
      = f.fields_for :preferences do |p|
        %tr
          %td
            = p.select :ranking, (1..@student.preferences.size), :include_blank => true
          %td
            = p.timeslot.day
          %td
            = p.object.timeslot.start_time.strftime("%l:%M %P")
          %td
            = p.object.timeslot.end_time.strftime("%l:%M %P")
            

  = f.submit("Submit Rankings")
  = link_to "skip this step", next_wizard_path 
