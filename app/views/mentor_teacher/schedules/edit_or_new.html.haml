.pull-right
  = render :partial => "form", :locals => {:submit_link => @submit_link, :method => @method}
.page-header
  %h1 Edit Schedule
= render :partial => "week_calendar", :locals => {:timeslots => @timeslots, :read_only => @read_only, :course_names => @course_names}       
  


