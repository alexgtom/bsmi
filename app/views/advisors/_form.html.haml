= form_for @advisor do |f|
  - if @advisor.errors.any?
    #error_explanation
      %h2= "#{pluralize(@advisor.errors.count, "error")} prohibited this advisor from being saved:"
      %ul
        - @advisor.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
