= form_for @student, url: wizard_path do |f|
  #timeslots
  %section
    %h2 Wednesday
    = render :partial => "day_selection", :locals => { :day => :wednesday, :calendar_id => :wednesday, :timeslots => Timeslot.where(:day => Timeslot.day_index(:wednesday))}
  = submit_tag("Submit Timeslots")
  = link_to "skip this step", next_wizard_path 
