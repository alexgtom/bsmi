.page-header
  %h1 Student Roster

%table.table.table-hover
  %tr
    %th Email
    -# %th
    %th

  - @all_advisor.each do |advisor|
    %tr
      %td= advisor.first_name
      %td= advisor.last_name
      %td= advisor.email 
      -# %td= link_to 'Edit', advisor
      %td= link_to 'Delete', advisor, method: :delete, data: { confirm: 'Are you sure?' }
%br

-# = link_to 'New Advisor', new_advisor_path
