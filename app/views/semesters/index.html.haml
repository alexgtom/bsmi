.pull-right
  = link_to 'New Semester', new_semester_path, :class => 'btn'
.page-header
  %h1 Semesters

%table.table.table-hover
  %tr
    %th Status
    %th Name
    %th Year
    %th Start date
    %th End date
    %th

  - @semesters.each do |semester|
    %tr
      %td= semester.status
      %td= link_to semester.name, edit_semester_path(semester)
      %td= semester.year
      %td= semester.start_date
      %td= semester.end_date
      %td= link_to 'Destroy', semester, method: :delete, data: { confirm: 'Are you sure?' }

